apiVersion: 1

providers:
  - name: 'Asistente Dashboards'
    orgId: 1
    folder: 'Asistente'
    folderUid: asistente
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
