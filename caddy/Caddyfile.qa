{
    email admin@example.com
}

{$DOMAIN:qa-admin.midominio.cl} {
    encode zstd gzip

    @backend {
        path /api/* /v3/* /swagger-ui/* /actuator/*
    }
    reverse_proxy @backend backend-java:8080

    @frontend {
        path /*
    }
    reverse_proxy @frontend frontend-react:8080
}
